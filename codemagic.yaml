workflows:
  android-release:
    name: SmartVillage Android Production Release
    max_build_duration: 60
    instance_type: mac_mini_m1
    environment:
      flutter: stable
      xcode: latest
      java: 17
      node: 20
      groups:
        - google_play # Set up in Codemagic UI if publishing automatically to Google Play
      vars:
        PACKAGE_NAME: "com.smartvillage.app"
    triggering:
      events:
        - push
        - tag
      branch_patterns:
        - pattern: 'main'
          include: true
          source: true
        - pattern: 'release/*'
          include: true
          source: true
      tag_patterns:
        - pattern: 'v*'
          include: true
          source: true
    scripts:
      - name: Set up Android environment and local.properties
        script: |
          echo "flutter.sdk=$FLUTTER_ROOT" > android/local.properties
          BUILD_NUM=${BUILD_NUMBER:-2}
          echo "flutter.buildMode=release" >> android/local.properties
          echo "flutter.versionName=1.1.0" >> android/local.properties
          echo "flutter.versionCode=$BUILD_NUM" >> android/local.properties
          echo "Building SmartVillage Android version 1.1.0 (build $BUILD_NUM)"
      - name: Build Web Frontend Assets
        script: |
          npm ci || npm install
          npm run build
      - name: Get Flutter dependencies
        script: |
          flutter packages pub get
      - name: Build Android App Bundle (AAB for Google Play)
        script: |
          BUILD_NUM=${BUILD_NUMBER:-2}
          flutter build appbundle --release --build-name=1.1.0 --build-number=$BUILD_NUM
      - name: Build Android Release APK (Direct Install for Field Testing)
        script: |
          BUILD_NUM=${BUILD_NUMBER:-2}
          flutter build apk --release --build-name=1.1.0 --build-number=$BUILD_NUM
    artifacts:
      - build/**/outputs/**/*.aab
      - build/**/outputs/**/*.apk
      - dist/**
    publishing:
      email:
        recipients:
          - ameenujaafar432@gmail.com
        notify:
          success: true
          failure: true

  web-production:
    name: SmartVillage Web Release
    max_build_duration: 30
    instance_type: mac_mini_m1
    environment:
      node: 20
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: 'main'
          include: true
    scripts:
      - name: Install dependencies
        script: |
          npm ci || npm install
          npm run build
    artifacts:
      - dist/**
    publishing:
      email:
        recipients:
          - ameenujaafar432@gmail.com
        notify:
          success: true
          failure: true
